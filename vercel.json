{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/api/:path*", "destination": "/server/server.js" }],
  "functions": {
    "server/server.js": {
      "runtime": "nodejs18.x"
    }
  }
}
